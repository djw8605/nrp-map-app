# Example environment variables for local development
# Add real values in a local .env file (do NOT commit .env)

# Public URL for R2 bucket where generated nodes are stored
# This will be exposed to the client as NEXT_PUBLIC_R2_URL by next.config.js
NODES_PUBLIC_URL=https://<bucket>.<account>.r2.cloudflarestorage.com

# Example of other vars you may need
# NETBOX_TOKEN=your-netbox-token
