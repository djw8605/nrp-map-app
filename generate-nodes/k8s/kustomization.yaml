apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - cronjob.yaml
  - rbac.yaml

secretGenerator:
  - name: generate-nodes-secrets
    envs:
      - secrets.env

images:
  - name: generate-nodes
    newTag: latest
